# Project: text pipeline

A small library for text normalization and parsing.

## Rules
- Python 3.12, type annotations are mandatory.
- Every public function has a docstring and a test in `tests/`.
- No external dependencies, standard library only.
- Style: 4 spaces, maximum line length 100.
- All answers, comments and docstrings must be in English.
